#include <stdio.h>

int main() {
    int n, sum ;
    printf("Enter a number: ");
    scanf("%d", &n);
    while (n > 0) {
        
        sum =n*(n+1)/2;
        
 printf("Sum of digits: %d\n", sum);
    break;
}
    return 0; 
}
